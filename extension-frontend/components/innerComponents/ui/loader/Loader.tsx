// import { useLoader } from "@src/hooks/useLoader";
// import { InfinitySpin } from "react-loader-spinner";

export const Loader = () => {
    // const { loading } = useLoader();
    return (
        <>
            {/* {loading && (
        <div className="fixed w-screen h-screen flex items-center justify-center overflow-hidden bg-background z-50">
          <InfinitySpin width="250" color="#FFFFFF" />
        </div>
      )} */}
        </>
    )
}
